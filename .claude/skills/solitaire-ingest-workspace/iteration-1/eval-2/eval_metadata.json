{
  "eval_id": 2,
  "eval_name": "heterogeneous-mixed-batch",
  "prompt": "Batch drop from the harvester, please ingest all of it: solitaire-win-5c25ad + solitaire-ai-log-5c25ad, solitaire-win-bcd6cf + solitaire-ai-log-bcd6cf, and solitaire-game-a11e74. They're all in Downloads.",
  "assertions": [
    "Classifies all three correctly: #5c25ad and #bcd6cf TERMINAL-WIN, #a11e74 PENDING-SNAPSHOT (not a loss)",
    "Flags the heterogeneous build (262774b vs f5c3870) and reads attribution per file",
    "Notes all three share prompt hybrid-v1.3, so the build delta is harness-side, not a prompt change",
    "Routes the two wins to '## Won sessions' and holds the snapshot as pending (records no outcome)",
    "Counts batch win-rate from terminal wins only (2/2), not treating the snapshot as a loss (not 2/3)",
    "Leads with per-session verdicts before deep analysis"
  ]
}